<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bfs.sorteiosapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ResultNumberDraw"></activity>
        <activity android:name=".OptionsDrawANumber" />
        <activity android:name=".UniqueResult" />
        <activity android:name=".ShowSavedParticipants" />
        <activity android:name=".StartScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddParticipants" />
        <activity android:name=".ResultIndividualDraw" />
        <activity android:name=".MultipleTeams" />
        <activity android:name=".Screen" />
        <activity android:name=".ResultMultipleDraw" />
        <activity android:name=".OptionsMultipleDraw" />
        <activity android:name=".IndividualDrawChooseType" />
    </application>

</manifest>